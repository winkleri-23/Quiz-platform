# Decoded Security — Path Finder

A 60-second diagnostic quiz that routes visitors to personalized article recommendations.

## Local development

```bash
npm install
npm run dev
```

Open http://localhost:5173

## Build

```bash
npm run build
```

Output goes to `/dist`.

## Deploy

Connected to Vercel via GitHub. Push to `main` to deploy.
